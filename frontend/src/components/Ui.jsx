export function Loading({ label = 'Loading records' }) { return <div className="loading"><span className="spinner" />{label}</div> }
export function ErrorState({ message }) { return <div className="error-state"><strong>Couldn’t connect to the engine.</strong><span>{message}</span><small>Showing local demo data where available.</small></div> }
export function TierBadge({ tier }) { return <span className={`tier tier-${tier?.toLowerCase()}`}>{tier}</span> }
export function EmptyState({ title = 'No records found', detail = 'Try adjusting your filters.' }) { return <div className="empty"><strong>{title}</strong><span>{detail}</span></div> }
export function StatCard({ label, value, detail, accent = '' }) { return <div className={`stat-card ${accent}`}><span className="stat-label">{label}</span><strong>{value}</strong><small>{detail}</small></div> }
export function SectionHeading({ eyebrow, title, action }) { return <div className="section-heading"><div><div className="eyebrow">{eyebrow}</div><h1>{title}</h1></div>{action}</div> }
export function Modal({ member, onClose }) { if (!member) return null; return <div className="modal-backdrop" onClick={onClose}><div className="modal" onClick={(event) => event.stopPropagation()}><button className="close" onClick={onClose} aria-label="Close">×</button><div className="member-hero"><span className="avatar avatar-large">{member.name?.split(' ').map((part) => part[0]).join('')}</span><div><div className="eyebrow">MEMBER PROFILE</div><h2>{member.name}</h2><span className="muted">{member.id} · {member.email}</span></div></div><div className="detail-grid"><div><small>Tier</small><TierBadge tier={member.tier} /></div><div><small>Available points</small><strong>{member.points_balance?.toLocaleString()}</strong></div><div><small>Lifetime points</small><strong>{member.lifetime_points?.toLocaleString()}</strong></div></div></div></div> }
